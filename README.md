# halbach-optimizer

Halbach配列（双極子近似）の最適化とモンテカルロ解析コード。

## Setup (Windows / PowerShell, Python 3.11)
```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -r requirements-dev.txt
